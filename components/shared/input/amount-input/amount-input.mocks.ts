import { IAmountInput } from './amount-input';

const base: IAmountInput = {
  placeholder: 'Hello world!',
  value: '',
  type: 'text',
  onChange: '',
};

export const mockAmountInputProps = {
  base,
};
