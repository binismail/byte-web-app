import { IPasswordInput } from './password-input';

const base: IPasswordInput = {
  placeholder: 'Hello world!',
  value: '',
  type: 'text',
  onChange: '',
};

export const mockPasswordInputProps = {
  base,
};
