import { IQuantityInput } from './quantity-input';

const base: IQuantityInput = {
  placeholder: 'Hello world!',
  value: '',
  type: 'text',
  onChange: '',
};

export const mockQuantityInputProps = {
  base,
};
