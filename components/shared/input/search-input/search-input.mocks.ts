import { ISearchInput } from './search-input';

const base: ISearchInput = {
  placeholder: 'Hello world!',
  value: '',
  type: 'text',
  onChange: '',
};

export const mockSearchInputProps = {
  base,
};
